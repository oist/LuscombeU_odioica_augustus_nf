{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/oist/luscombeu_odioica_augustus_nf/master/assets/schema_input.json",
    "title": "oist/luscombeu_odioica_augustus_nf pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "genome": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.fn?a(sta)?(\\.gz)?$",
                "errorMessage": "Genome FASTA file must be provided, cannot contain spaces and must have extension '.fa', '.fasta', '.fna', or '.fa.gz'"
            },
            "trans": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^([\\S\\s]*\\/)?[^\\s\\/]+\\.fn?a(sta)?(\\.gz)?$",
                "errorMessage": "Transcriptome FASTA file must be provided, cannot contain spaces and must have extension '.fa', '.fasta', '.fna', or '.fa.gz'"
            },
            "species": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Species name must be provided and cannot contain spaces (e.g. oikopleura_dioica_okinawa_1)",
                "meta": ["species"]
            }
        },
        "required": ["sample", "genome", "trans", "species"]
    }
}
